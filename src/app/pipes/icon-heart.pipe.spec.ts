import { IconHeartPipe } from './icon-heart.pipe';

describe('IconHeartPipe', () => {
  it('create an instance', () => {
    const pipe = new IconHeartPipe();
    expect(pipe).toBeTruthy();
  });
});
